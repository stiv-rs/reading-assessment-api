spring:
  application:
    name: reading-assessment-api
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

gemini:
  api-key: ${GEMINI_API_KEY}
  api-url: ${GEMINI_API_URL}